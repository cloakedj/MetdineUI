import { DisableInputDirective } from './disable-input.directive';

describe('DisableInputDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableInputDirective();
    expect(directive).toBeTruthy();
  });
});
