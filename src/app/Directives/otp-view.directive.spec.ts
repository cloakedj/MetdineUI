import { OtpViewDirective } from './otp-view.directive';

describe('OtpViewDirective', () => {
  it('should create an instance', () => {
    const directive = new OtpViewDirective();
    expect(directive).toBeTruthy();
  });
});
