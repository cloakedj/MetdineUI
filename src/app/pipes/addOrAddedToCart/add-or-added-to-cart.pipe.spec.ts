import { AddOrAddedToCartPipe } from './add-or-added-to-cart.pipe';

describe('AddOrAddedToCartPipe', () => {
  it('create an instance', () => {
    const pipe = new AddOrAddedToCartPipe();
    expect(pipe).toBeTruthy();
  });
});
