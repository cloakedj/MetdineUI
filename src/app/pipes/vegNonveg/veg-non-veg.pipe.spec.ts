import { VegNonVegPipe } from './veg-non-veg.pipe';

describe('VegNonVegPipe', () => {
  it('create an instance', () => {
    const pipe = new VegNonVegPipe();
    expect(pipe).toBeTruthy();
  });
});
