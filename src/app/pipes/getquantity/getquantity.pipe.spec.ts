import { GetquantityPipe } from './getquantity.pipe';

describe('GetquantityPipe', () => {
  it('create an instance', () => {
    const pipe = new GetquantityPipe();
    expect(pipe).toBeTruthy();
  });
});
